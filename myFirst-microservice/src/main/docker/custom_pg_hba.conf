# Unix socket
local   all             all                                     scram-sha-256

# IPv4 localhost
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 localhost
host    all             all             ::1/128                 scram-sha-256

# Allow Docker network connections with password encryption
host    all             all             172.18.0.0/16           scram-sha-256



